import React from 'react';
import ReactDOM from 'react-dom';
import App from './routerConfig.js';
import './common/less/index.less';

ReactDOM.render(
  // <React.StrictMode>
    <App/>,
  // </React.StrictMode>,
  document.getElementById('root')
)
